<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.spring.board.model.mapper.BoardMapper">
	
	<sql id="boardColumns">
		SELECT
		       BOARD_NO boardNo
		     , CATEGORY_NAME category
		     , NICKNAME boardWriter
		     , BOARD_TITLE boardTitle
		     , BOARD_CONTENT boardContent
		     , VIEW_COUNT viewCount
		     , CT_BOARD.ENROLL_DATE enrollDate
		     , (SELECT 
		               COUNT(*)
		          FROM
		               CT_BOARD_LIKE 
		         WHERE
		               CT_BOARD_LIKE.BOARD_NO = CT_BOARD.BOARD_NO) AS likes
		  FROM
		       CT_BOARD
		  JOIN
		       CT_MEMBER USING (USER_NO)
		  JOIN
		       CT_BOARD_CATEGORY USING (CATEGORY_NO)
		 WHERE
		       STATUS = 'Y'
	</sql>
	
	<sql id="boardCount">
		SELECT
			   COUNT(*)
		  FROM
		       CT_BOARD
		  JOIN
		       CT_MEMBER USING (USER_NO)
		 WHERE
		       STATUS = 'Y'
	</sql>
	
	
	<select id="selectBoardCount"
			resultType="_int" parameterType="hashmap">
		<include refid="boardCount"/>		
		<if test="category != 0">
		   AND 
		       CATEGORY_NO = #{category}
		 </if>
	</select>
	
	<select id="selectBoardList" resultType="com.kh.spring.board.model.dto.BoardDTO" parameterType="hashmap">
		<include refid="boardColumns"></include>
		 <if test="category != 0">
		   AND 
		       CATEGORY_NO = #{category}
		 </if>
		 ORDER
		    BY
		       <choose>
		        <when test="orderBy == 'likes'">
		         likes DESC
		        </when>
		        <when test="orderBy == 'boardTitle'">
		         boardTitle ASC
		        </when>
		        <when test="orderBy == 'viewCount'">
		         viewCount DESC
		        </when>
		        <when test="orderBy == 'boardWriter'">
		         boardWriter ASC
		        </when>
		        <when test="orderBy == 'boardNo'" >
		         boardNo DESC
		        </when>
		       	<otherwise>
		       	 enrollDate DESC 
		       	</otherwise>
		       </choose>
	</select>
	
	<select id="selectBoardCountByKeyword"
			resultType="_int" parameterType="hashmap">
		<include refid="boardCount"/>
		<if test="category != 0">
		   AND 
		       CATEGORY_NO = #{category}
		 </if>
		   AND
		   <choose>
			<when test ="condition == 'title'">
		 	   BOARD_TITLE
		 	</when>
		 	<when test="condition == 'content'">
		 	   BOARD_CONTENT
		 	</when>
		 	<otherwise>
		 	   NICKNAME
		 	</otherwise>
		 	</choose>
		   	   LIKE '%' || #{keyword} || '%'
	</select>
	
	<select id="selectBoardListByKeyword" resultType="com.kh.spring.board.model.dto.BoardDTO" parameterType="hashmap">
		<include refid="boardColumns" />
		 <if test="category != 0">
		   AND 
		       CATEGORY_NO = #{category}
		 </if>
		   AND
		 <choose>
		 	<when test ="condition == 'title'">
		 	   BOARD_TITLE 
		 	</when>
		 	<when test="condition == 'content'">
		 	   BOARD_CONTENT
		 	</when>
		 	<otherwise>
		 	   NICKNAME
		 	</otherwise>
		 </choose>
		       LIKE '%' || #{keyword} || '%'
		 ORDER
		    BY
		       <choose>
		        <when test="orderBy == 'likes'">
		         likes DESC
		        </when>
		        <when test="orderBy == 'boardTitle'">
		         boardTitle ASC
		        </when>
		        <when test="orderBy == 'viewCount'">
		         viewCount DESC
		        </when>
		        <when test="orderBy == 'boardWriter'">
		         boardWriter ASC
		        </when>
		        <when test="orderBy == 'boardNo'" >
		         boardNo DESC
		        </when>
		       	<otherwise>
		       	 enrollDate DESC 
		       	</otherwise>
		       </choose>
	</select>
	
	
	<update id="increaseBoardCount" parameterType="long">
		UPDATE
		       CT_BOARD
		   SET
		       VIEW_COUNT = VIEW_COUNT + 1
		 WHERE
		       BOARD_NO = #{boardNo}
		   AND
		       STATUS = 'Y'
	</update>
	
	
	<resultMap type="com.kh.spring.board.model.dto.BoardDTO" id="selectBoardResult">
		<id column="boardNo" property="boardNo"/>
		<result column="boardTitle" property="boardTitle"/>
		<result column="boardContent" property="boardContent"/>
		<result column="boardWriter" property="boardWriter"/>
		<result column="viewCount" property="viewCount"/>
		<result column="category" property="category"/>
		<result column="boardEnrollDate" property="enrollDate"/>
		<result column="likes" property="likes"/>
		<association property="attachment" javaType="com.kh.spring.board.model.dto.AttachmentDTO">
			<id column="fileNo" property="fileNo" />
			<result column="originName" property="originName"/>
			<result column="changeName" property="changeName"/>
			<result column="filePath" property="filePath"/>
			<result column="attachmentStatus" property="status" />
		</association>
		<collection property="replies" ofType="com.kh.spring.board.model.dto.ReplyDTO">
			<id column="replyNo" property="replyNo"/>
			<result column="replyContent" property="replyContent"/>
			<result column="replyWriter" property="replyWriter"/>
			<result column="replyEnrollDate" property="enrollDate"/>
			<result column="replyStatus" property="status" />
		</collection>
	</resultMap>
	  
	  
	<select id="selectByBoardNo" parameterType="long" resultMap="selectBoardResult">
	SELECT
	       CT_BOARD.BOARD_NO boardNo
	     , BOARD_TITLE boardTitle
	     , BOARD_CONTENT boardContent
	     , BOARDMEMBER.NICKNAME boardWriter
	     , VIEW_COUNT viewCount
	     , CATEGORY_NAME category
	     , CT_BOARD.ENROLL_DATE boardEnrollDate
	     , FILE_NO fileNo
	     , ORIGIN_NAME originName
	     , CHANGE_NAME changeName
	     , FILE_PATH filePath
	     , CT_BOARD_ATTACHMENT.STATUS attachmentStatus
	     , REPLY_NO replyNo
	     , REPLY_CONTENT replyContent
	     , REPLYMEMBER.NICKNAME replyWriter
	     , CT_BOARD_REPLY.ENROLL_DATE replyEnrollDate
	     , CT_BOARD_REPLY.STATUS replyStatus
	     , (SELECT 
	               COUNT(*)
	     	  FROM
	     	       CT_BOARD_LIKE
	     	 WHERE
	     	       CT_BOARD.BOARD_NO = CT_BOARD_LIKE.BOARD_NO) AS LIKES
	  FROM
	       CT_BOARD
	  JOIN
	       CT_BOARD_CATEGORY USING (CATEGORY_NO)
	  JOIN
	       CT_MEMBER BOARDMEMBER USING (USER_NO)
	  LEFT
	  JOIN
	       CT_BOARD_ATTACHMENT ON (BOARD_NO = REF_BNO) 
	  LEFT
	  JOIN 
	       CT_BOARD_REPLY ON (CT_BOARD.BOARD_NO = CT_BOARD_REPLY.BOARD_NO)
	  LEFT
	  JOIN
	       CT_MEMBER REPLYMEMBER ON (REPLYMEMBER.USER_NO = CT_BOARD_REPLY.USER_NO)
	 WHERE
	       CT_BOARD.STATUS = 'Y'
	   AND
	       CT_BOARD.BOARD_NO = #{boardNo}
	 ORDER
	    BY
	       replyEnrollDate DESC
	
	</select>
	<!-- 댓글 작성자로 참조할 외래키 값이 USER_NO(NUMBER)라서 댓글 작성자의 NICKNAME 값을 따로 출력해 주기 위해
		 CT_MEMBER를 한번 더 조인   -->
	<insert id="insertAttachment" parameterType="com.kh.spring.board.model.dto.AttachmentDTO">
	
		INSERT
		  INTO
		       CT_BOARD_ATTACHMENT
		VALUES
		       (
		       SEQ_BOARD_FILE_NO.NEXTVAL
		     , #{refBno}
		     , #{originName}
		     , #{changeName}
		     , #{filePath}
		     , SYSDATE
		     , DEFAULT
		       )
	</insert> 
		 
	<insert id="insertBoard" parameterType="com.kh.spring.board.model.dto.BoardDTO">
		<selectKey keyProperty="boardNo" resultType="long" order="BEFORE">
			SELECT SEQ_BOARD.NEXTVAL FROM DUAL
		</selectKey>
		
		INSERT
		  INTO
		       CT_BOARD
		VALUES
		       (
		       #{boardNo}
		     , #{category}
		     , #{boardWriter}
		     , #{boardTitle}
		     , #{boardContent}
		     , 0
		     , SYSDATE
		     , 'Y'
		       )
	</insert> 
	
	<update id="updateBoard" parameterType="com.kh.spring.board.model.dto.BoardDTO">
		UPDATE
		       CT_BOARD
		   SET
		       CATEGORY_NO = #{category}
		     , BOARD_TITLE = #{boardTitle}
		     , BOARD_CONTENT = #{boardContent}
		 WHERE
		       BOARD_NO = #{boardNo}
		   AND
		       STATUS = 'Y'
	
	</update>

	<update id="updateAttachment" parameterType="com.kh.spring.board.model.dto.AttachmentDTO">
		UPDATE
		       CT_BOARD_ATTACHMENT
		   SET
		       ORIGIN_NAME = #{originName}
		     , CHANGE_NAME = #{changeName}
		 WHERE
		       FILE_NO = #{fileNo}
	</update>

	<update id="deleteBoard" parameterType="com.kh.spring.board.model.dto.BoardDTO">
	
		UPDATE
		       CT_BOARD
		   SET
		       STATUS = 'N'
		 WHERE
		       BOARD_NO = #{boardNo}
		   AND
		       USER_NO = #{boardWriter}
	
	</update>
	
	<update id="deleteAttachment" parameterType="com.kh.spring.board.model.dto.AttachmentDTO">
		UPDATE
		       CT_BOARD_ATTACHMENT
		   SET
		       STATUS = 'N'
		 WHERE
	           FILE_NO = #{fileNo}
	</update>

	<select id="selectLikes" parameterType="com.kh.spring.board.model.dto.LikeDTO" 
	resultType ="com.kh.spring.board.model.dto.LikeDTO">
		SELECT
		       USER_NO userNo
		     , BOARD_NO boardNo
		  FROM
		       CT_BOARD_LIKE
		 WHERE
		       USER_NO = #{userNo}
		   AND
		       BOARD_NO = #{boardNo}
	
	</select>

	<insert id="insertLikes" parameterType="com.kh.spring.board.model.dto.LikeDTO" >
		INSERT
		  INTO
		       CT_BOARD_LIKE
		VALUES
		       (
		       #{userNo}
		     , #{boardNo}
		       )
	
	</insert>
	
	<delete id="deleteLikes" parameterType="com.kh.spring.board.model.dto.LikeDTO">
		DELETE
		  FROM
		       CT_BOARD_LIKE
		 WHERE
		       USER_NO = #{userNo}
		   AND
		       BOARD_NO = #{boardNo}
	
	</delete>
	
	<insert id="insertReply" parameterType="com.kh.spring.board.model.dto.ReplyDTO">
		INSERT
		  INTO
		       CT_BOARD_REPLY
		VALUES
		       (
		       SEQ_BOARD_REPLY.NEXTVAL
		     , #{refBno}
		     , #{replyWriter}
		     , #{replyContent}
		     , SYSDATE
		     , 'Y'
		       )
		
	
	</insert>
	
	<update id="updateReply" parameterType="com.kh.spring.board.model.dto.ReplyDTO">
		UPDATE
			   CT_BOARD_REPLY
		   SET
		       REPLY_CONTENT = #{replyContent}
		 WHERE
		 	   REPLY_NO = #{replyNo}
		   AND
		       USER_NO = #{replyWriter}
		   AND
		       BOARD_NO = #{refBno}
		   AND
		       STATUS = 'Y'
	
	</update>

</mapper>