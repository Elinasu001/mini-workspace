<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.spring.event.model.mapper.EventMapper">

  <!-- 상세조회용 resultMap -->
  <resultMap id="eventResultSet" type="eventdto">
    <id property="eventNo" column="EVENT_NO"/>
    <result property="eventTitle" column="EVENT_TITLE"/>
    <result property="eventContent" column="EVENT_CONTENT"/>
    <result property="startDate" column="START_DATE"/>
    <result property="endDate" column="END_DATE"/>
    <result property="viewCount" column="VIEW_COUNT"/>
    <result property="status" column="STATUS"/>
    <result property="userNo" column="USER_NO"/>
    <result property="userId" column="USER_ID"/>

    <association property="category" javaType="eventcategory">
      <id property="categoryNo" column="CATEGORY_NO"/>
      <result property="categoryName" column="CATEGORY_NAME"/>
    </association>

    <collection property="files" ofType="eventattachment">
      <id property="fileNo" column="FILE_NO"/>
      <result property="originName" column="ORIGIN_NAME"/>
      <result property="changeName" column="CHANGE_NAME"/>
      <result property="filePath" column="FILE_PATH"/>
      <result property="fileLevel" column="FILE_LEVEL"/>
    </collection>
  </resultMap>

  <!-- 목록용 resultMap -->
  <resultMap id="eventListResultSet" type="eventdto">
    <id property="eventNo" column="EVENT_NO"/>
    <result property="eventTitle" column="EVENT_TITLE"/>
    <result property="startDate" column="START_DATE"/>
    <result property="endDate" column="END_DATE"/>
    <result property="thumbnailPath" column="FILE_PATH"/>
    <result property="thumbnailName" column="CHANGE_NAME"/>

    <association property="category" javaType="eventcategory">
      <id property="categoryNo" column="CATEGORY_NO"/>
      <result property="categoryName" column="CATEGORY_NAME"/>
    </association>

  </resultMap>

  <!-- 진행중인 이벤트 개수 -->
  <select id="selectOngoingCount" resultType="_int">
    SELECT 
           COUNT(*) 
      FROM 
           CT_EVENT
     WHERE 
           STATUS = 'Y'
       AND 
           END_DATE >= SYSDATE
  </select>

  <!-- 진행중인 이벤트 목록 -->
  <select id="selectOngoing" resultMap="eventListResultSet">
    SELECT
           E.EVENT_NO
         , E.EVENT_TITLE
         , E.START_DATE
         , E.END_DATE
         , C.CATEGORY_NO
         , C.CATEGORY_NAME
         , A.FILE_PATH
         , A.CHANGE_NAME
      FROM 
           CT_EVENT E
      JOIN 
           CT_EVENT_CATEGORY C ON (E.CATEGORY_NO = C.CATEGORY_NO)
      LEFT 
      JOIN 
           CT_EVENT_ATTACHMENT A ON (A.REF_BNO = E.EVENT_NO AND A.FILE_LEVEL = 0)
     WHERE 
           E.STATUS = 'Y'
       AND 
           E.END_DATE >= SYSDATE
     ORDER 
        BY 
           E.EVENT_NO DESC
  </select>

  <!-- 종료된 이벤트 개수 -->
  <select id="selectEndedCount" resultType="_int">
    SELECT 
    	   COUNT(*) 
      FROM 
           CT_EVENT
     WHERE 
           STATUS = 'Y'
       AND 
           END_DATE &lt; SYSDATE
  </select>

  <!-- 종료된 이벤트 목록 -->
  <select id="selectEnded" resultMap="eventListResultSet">
    SELECT
           E.EVENT_NO
         , E.EVENT_TITLE
         , E.START_DATE
         , E.END_DATE
         , C.CATEGORY_NO
         , C.CATEGORY_NAME
         , A.FILE_PATH
         , A.CHANGE_NAME
      FROM 
      	   CT_EVENT E
      JOIN 
           CT_EVENT_CATEGORY C ON (E.CATEGORY_NO = C.CATEGORY_NO)
      LEFT 
      JOIN 
           CT_EVENT_ATTACHMENT A ON (A.REF_BNO = E.EVENT_NO AND A.FILE_LEVEL = 0)
     WHERE 
           (E.STATUS = 'Y' AND E.END_DATE &lt; SYSDATE)
        OR 
           E.STATUS = 'N'
     ORDER 
        BY 
           E.EVENT_NO DESC
  </select>

  <!-- 이벤트 상세조회 -->
  <select id="selectByEventNo" parameterType="_long" resultMap="eventResultSet">
    SELECT 
           E.EVENT_NO
         , E.CATEGORY_NO
         , E.EVENT_TITLE
         , E.EVENT_CONTENT
         , E.START_DATE
         , E.END_DATE
         , E.VIEW_COUNT
         , E.STATUS
         , E.USER_NO
         , M.USER_ID
         , C.CATEGORY_NAME
         , A.FILE_NO
         , A.ORIGIN_NAME
         , A.CHANGE_NAME
         , A.FILE_PATH
         , A.FILE_LEVEL
         , A.STATUS AS FILE_STATUS
      FROM 
           CT_EVENT E
      JOIN 
           CT_EVENT_CATEGORY C ON (E.CATEGORY_NO = C.CATEGORY_NO)
      JOIN 
           CT_MEMBER M ON (E.USER_NO = M.USER_NO)
      LEFT 
      JOIN 
           CT_EVENT_ATTACHMENT A ON (A.REF_BNO = E.EVENT_NO)
     WHERE 
           E.EVENT_NO = #{eventNo}
       AND 
           E.STATUS = 'Y'
     ORDER 
        BY 
           A.FILE_LEVEL ASC
  </select>

	<!-- 조회수 증가 -->
	<update id="increaseCount" parameterType="long">
		UPDATE
		       CT_EVENT
		   SET
		       VIEW_COUNT = VIEW_COUNT + 1
		 WHERE
		       EVENT_NO = #{eventNo}
		   AND
		       STATUS = 'Y'
	</update>


	<!-- 이벤트 등록 -->
	<insert id="insertEvent" parameterType="eventdto">

	    <!-- 시퀀스 미리 조회해서 eventNo에 세팅 -->
	    <selectKey resultType="long" keyProperty="eventNo" order="BEFORE">
	        SELECT SEQ_EVENT.NEXTVAL FROM DUAL
	    </selectKey>

	    INSERT 
	      INTO 
	           CT_EVENT 
	           (
	           EVENT_NO
	         , CATEGORY_NO
	         , EVENT_TITLE
	         , EVENT_CONTENT
	         , START_DATE
	         , END_DATE
	         , STATUS
	         , USER_NO
	         , ENROLL_DATE
	    	   )
	    VALUES (
	           #{eventNo}
	         , #{category.categoryNo}
	         , #{eventTitle}
	         , #{eventContent}
	         , #{startDate}
	         , #{endDate}
	         , 'Y'
	         , #{userNo}
	         , DEFAULT
	    	   )
	</insert>

	<!-- 파일 첨부 -->
	<insert id="insertAttachment" parameterType="eventattachment">

	    <!-- 파일 시퀀스 자동 부여 -->
	    <selectKey resultType="long" keyProperty="fileNo" order="BEFORE">
	        SELECT SEQ_EVENT_FILE_NO.NEXTVAL FROM DUAL
	    </selectKey>
	
	    INSERT 
	      INTO 
	           CT_EVENT_ATTACHMENT 
	           (
	           FILE_NO
	         , REF_BNO
	         , ORIGIN_NAME
	         , CHANGE_NAME
	         , FILE_PATH
	         , FILE_LEVEL
	         , UPLOAD_DATE
	         , STATUS
	    	   ) 
	    VALUES (
	           #{fileNo}
	         , #{refBno}
	         , #{originName}
	         , #{changeName}
	         , #{filePath}
	         , #{fileLevel}
	         , DEFAULT
	         , 'Y'
	           )
	</insert>
	
	
	<!-- 이벤트 수정 -->
    <update id="updateEvent" parameterType="eventdto">
        UPDATE 
               CT_EVENT
           SET 
               CATEGORY_NO   = #{category.categoryNo}
             , EVENT_TITLE   = #{eventTitle}
             , EVENT_CONTENT = #{eventContent}
             , START_DATE    = #{startDate}
             , END_DATE      = #{endDate}
         WHERE 
               EVENT_NO = #{eventNo}
           AND 
               STATUS = 'Y'
    </update>
	
	<!-- 카테고리 목록 조회 -->
  	<select id="selectCategoryList" resultType="eventcategory">
	    SELECT 
	    	   CATEGORY_NO		categoryNo
	    	 , CATEGORY_NAME	categoryName
	      FROM 
	           CT_EVENT_CATEGORY
	     ORDER 
	        BY 
	           CATEGORY_NO ASC
    </select>

	<!-- 첨부파일 단일 조회 (삭제용) -->
    <select id="selectAttachmentByLevel" resultType="eventattachment">
        SELECT 
        	   FILE_NO		fileNo
        	 , REF_BNO		refBno
        	 , FILE_PATH	filePath
        	 , CHANGE_NAME	changeName
        	 , FILE_LEVEL	fileLevel
        	 , STATUS
          FROM 
               CT_EVENT_ATTACHMENT
         WHERE 
               REF_BNO = #{eventNo}
           AND 
               FILE_LEVEL = #{fileLevel}
           AND 
               STATUS = 'Y'
    </select>
	
	
	<!-- 기존 첨부파일 삭제 (DB 상태값만 N으로 변경) -->
	<update id="deleteAttachment" parameterType="long">
	    UPDATE 
	           CT_EVENT_ATTACHMENT
	       SET 
	           STATUS = 'N'
	     WHERE 
	           FILE_NO = #{fileNo}
	</update>
	
	<!-- 이벤트 삭제 (상태값 변경) -->
	<update id="deleteEvent" parameterType="long">
	    UPDATE 
	           CT_EVENT
	       SET 
	           STATUS = 'N'
	     WHERE 
	           EVENT_NO = #{eventNo}
	</update>
	
	<!-- 메인 페이지 진행 중 이벤트 조회 : 진행 중(status='Y') + 기간 유효한 이벤트 + 상위 3개만 조회-->
	<select id="selectEventOngoingTop" resultMap="eventListResultSet">
	    SELECT
	           E.EVENT_NO
	         , E.EVENT_TITLE
	         , E.START_DATE
	         , E.END_DATE
	         , C.CATEGORY_NO
	         , C.CATEGORY_NAME
	         , A.FILE_PATH
	         , A.CHANGE_NAME
	      FROM 
	           CT_EVENT E
	      JOIN 
	           CT_EVENT_CATEGORY C ON (E.CATEGORY_NO = C.CATEGORY_NO)
	      LEFT 
	      JOIN 
	           CT_EVENT_ATTACHMENT A ON (A.REF_BNO = E.EVENT_NO AND A.FILE_LEVEL = 0)
	     WHERE 
	           E.STATUS = 'Y'
	       AND 
	           E.START_DATE &lt;= SYSDATE  <!-- 시작일 오늘 이전 -->
		   AND 
		       E.END_DATE >= SYSDATE	<!-- 종료일 오늘 이후 -->
	     ORDER 
	        BY 
	           E.EVENT_NO DESC
	     FETCH FIRST 3 ROWS ONLY
	</select>
	
    

</mapper>
